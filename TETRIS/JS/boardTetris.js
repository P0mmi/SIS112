import { Grid } from '/JS/grid.js'

export class BoardTetris extends Grid{
    constructor(canvas, filas, columnas, cellSize, space) {
        super(canvas, filas, columnas, cellSize, space);
        
    }
}